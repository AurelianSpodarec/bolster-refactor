export function inviteClientSuccessMessage(hierarchy) {
    return `Your client has now been invited to view your ${hierarchy}. They will receive an email to accept this invite and guidance on their next steps.`;
}
