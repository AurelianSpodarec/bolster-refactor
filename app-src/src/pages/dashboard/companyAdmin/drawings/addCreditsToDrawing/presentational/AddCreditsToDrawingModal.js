import React from 'react';
import AddCreditsToDrawingFormContainer from '../containers/AddCreditsToDrawingFormContainer';

const AddCreditsToDrawingModal = ({ drawing }) => (
    <AddCreditsToDrawingFormContainer drawing={drawing} />
);

export default AddCreditsToDrawingModal;
