import React from 'react';

import Breadcrumb from '../presentational/Breadcrumb';

const BreadcrumbContainer = ({ classNames }) => (
    <Breadcrumb classNames={classNames} />
);

export default BreadcrumbContainer;
