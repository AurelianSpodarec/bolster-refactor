import React from 'react';
import CompanyReportsListItem from '../presentational/CompanyReportsListItem';

const CompanyReportsListItemContainer = ({ queueItem }) => (
    <CompanyReportsListItem queueItem={queueItem} />
);

export default CompanyReportsListItemContainer;
