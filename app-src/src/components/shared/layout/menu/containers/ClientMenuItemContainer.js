import React, { useMemo } from 'react';

import MenuItem from '../presentational/MenuItem';

const CompanyMenuItemContainer = ({ item }) => {
    return <MenuItem item={item} />;
};

export default CompanyMenuItemContainer;
